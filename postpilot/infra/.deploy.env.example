# The deployment workflow writes an immutable digest here on the VPS.
# For a first local build, use: postpilot-engine:local
ENGINE_IMAGE=ghcr.io/your-github-user/your-repository-engine@sha256:CHANGE_ME

