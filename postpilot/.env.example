# This root file is a variable checklist, not a runtime file.
#
# Runtime templates live at:
#   apps/web/.env.example   -> copy to apps/web/.env.local for local UI work
#   infra/.env.example      -> copy to infra/.env on the VPS
#   infra/.deploy.env.example -> copy to infra/.deploy.env on the VPS
#
# Never commit any copied runtime file or real credential.

